http:
  routers:
    dev:
      rule: "PathPrefix(`/`)"
      service: dev
      entryPoints:
        - web

  services:
    dev:
      loadBalancer:
        servers:
          - url: "{{ .Scheme }}://localhost:{{ .Port }}"